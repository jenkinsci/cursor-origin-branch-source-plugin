<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core">
    <j:choose>
      <j:when test="${it.displayName == null}">
        ${%Requested_by_app(it.namespace, it.appId)}
      </j:when>
      <j:otherwise>
          ${%Requested_by_app_with_displayName(it.namespace, it.appId, it.displayName)}
      </j:otherwise>
    </j:choose>
</j:jelly>
